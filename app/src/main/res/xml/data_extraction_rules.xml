<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12+ backup/transfer rules. We opt OUT of everything: the encrypted
  Room DB and any export files must never leave the device via cloud backup or
  device-to-device transfer. Combined with allowBackup="false" this closes the
  Auto-Backup gap that would otherwise silently copy private storage to Google.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
    </device-transfer>
</data-extraction-rules>
